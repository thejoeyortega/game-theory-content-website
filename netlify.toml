[build]
  publish = "."

[build.processing]
  skip_processing = false

[build.processing.html]
  pretty_urls = true

# Disable email obfuscation
[[headers]]
  for = "/*"
  [headers.values]
    X-Content-Type-Options = "nosniff"
